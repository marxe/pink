config = {
    'host': 'localhost',    #name of host to connect to
    'user': 'root',         #user to authenticate as
    'passwd': '',           #password to authenticate with
    'db': 'pink',           #database to use
}